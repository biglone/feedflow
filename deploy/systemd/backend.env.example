# Copy to: ~/.config/feedflow/backend.env (chmod 600)
#
# Optional runtime env for systemd backend service.
# Useful when this host needs an HTTP(S) proxy to reach Google/YouTube.

https_proxy=http://127.0.0.1:7890
http_proxy=http://127.0.0.1:7890
all_proxy=socks5://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
HTTP_PROXY=http://127.0.0.1:7890
ALL_PROXY=socks5://127.0.0.1:7890
NO_PROXY=127.0.0.1,localhost
