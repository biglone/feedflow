# Copy to: ~/.config/feedflow/backend.env (chmod 600)
#
# Optional runtime env for systemd backend service.
# Useful when this host needs an HTTP(S) proxy to reach Google/YouTube.

https_proxy=http://127.0.0.1:7890
http_proxy=http://127.0.0.1:7890
all_proxy=socks5://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
HTTP_PROXY=http://127.0.0.1:7890
ALL_PROXY=socks5://127.0.0.1:7890
NO_PROXY=127.0.0.1,localhost

# yt-dlp cookies (recommended when /api/youtube/stream hits bot-check)
# Store cookies outside the repo and keep permissions strict (chmod 600).
# Example:
# YTDLP_COOKIES_PATH=/home/your-user/.config/feedflow/yt-dlp-cookies.txt

# YouTube stream health check video ID (optional)
# YOUTUBE_HEALTHCHECK_VIDEO_ID=dQw4w9WgXcQ
