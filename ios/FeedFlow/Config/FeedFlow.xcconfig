// Shared build settings for FeedFlow.
// Add per-machine signing overrides in `FeedFlow.local.xcconfig` (ignored by git).

FEEDFLOW_BUNDLE_ID = com.feedflow.app
FEEDFLOW_DEVELOPMENT_TEAM =

PRODUCT_BUNDLE_IDENTIFIER = $(FEEDFLOW_BUNDLE_ID)
DEVELOPMENT_TEAM = $(FEEDFLOW_DEVELOPMENT_TEAM)
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY = Apple Development
PROVISIONING_PROFILE_SPECIFIER =

#include? "FeedFlow.local.xcconfig"
